# Changelog

## Не выпущено

- Секция «Система Nexor» (`#nexor-system`) вынесена в template-part `home-nexor-system-section.php` с рендер-хелпером `nexor_render_home_nexor_system_section()`; мигрированная разметка заменяется при рендере (иначе блок вставляется перед «до/после»), якорь `#nexor-system` сохранён.
- Секция «Система Nexor» перевёрстана по референсу: композиция вокруг рабочего чертежа планировки (`assets/nexor-system-floor-plan.jpg/.webp`) — крупный serif-заголовок и лид слева, четыре выноски-подписи с линиями-указателями к точкам на чертеже (десктоп), карточка-цитата с кавычкой снизу слева. На планшете/мобильном — стек: заголовок → чертёж → сетка из четырёх пунктов → цитата. Раскладка десктопа масштабируется через container query (`cqw`), фиксированных px-размеров для блоков нет. Интерактивные карточки-схема и их JS-гидрация (`setupSystemBlueprint`, классы `nexor-blueprint-*`) удалены.
- Позиционирование калькулятора и блоков «смета/цены/сроки» на главной переведено с текстового якоря «Ремонт без неприятных сюрпризов» на стабильный `id="nexor-system"` (старый текст заголовка секции больше не используется как маркер вставки).
- FAQ главной (`#faq`) вынесен в template-part; мигрированная разметка заменяется при рендере, якорь и тексты вопросов сохранены.
- Бонусы для клиентов (`#promotions`) вынесены в template-part; разметка, классы и якорь сохранены, данные по-прежнему из option `nexor_promotions`.
- Секция бонусов (`#promotions`) собрана по референсу: обратный отсчёт заменён статичным дедлайном «Только до …», featured-карточка слева и сетка постоянных бонусов справа.
- Заявки защищены невидимой Yandex SmartCaptcha: обычный посетитель ничего не нажимает, подозрительный трафик получает задание. Токен проверяется на сервере до записи CPT и отправки в Telegram. Ключ клиента — **Настройки → Nexor**, ключ сервера — `NEXOR_SMARTCAPTCHA_SERVER_KEY`. Лимит отправки — 3 заявки / 15 мин с IP.
- Секция FAQ перевёрстана по референсу: одноколоночный аккордеон с таймлайном, нумерацией 01–08, плюс/минус и акцентной подписью первого слова заголовка.
- Секция «Этапы» на главной показывает один этап за раз: медный ползунок сверху круга крутится мышкой/пальцем по часовой стрелке и плавно сменяет изображение и текст этапа (GSAP Draggable + InertiaPlugin, доводка по 72°).
- Пронумерованные этапы под карточкой работают как пагинация: подсвечивают активный этап и доворачивают круг к выбранному этапу; добавлены клавиатурное управление ползунком (стрелки, Home/End) и дуга прогресса.
- Пока посетитель не взялся за ползунок, он пружинит по кругу с паузами (не дальше половины шага, поэтому этап не меняется), а при первом появлении секции рядом всплывает подсказка «Потяните по кругу». Анимация останавливается на первом взаимодействии, замирает вне экрана и не запускается при `prefers-reduced-motion`.
- Удалена старая секция «Как мы делаем ремонт предсказуемым» (`nexor-process-section` / `#work-stages`) — её дублировала новая интерактивная карточка этапов. Блок вырезается из уже сохранённого контента главной при рендере, удалён из seed-разметки, а его JS и CSS убраны из темы.
- Секция «Как нам это удаётся?» (`#budget-control`): подпись под показателем «0%» перенесена из правой колонки под саму цифру и отделена тонкой линией — по референсу. Заодно устранены задвоенные и частично мёртвые mobile-правила `.nexor-budget__*` в `nexor.css`.

## 1.6.0 — 2026-07-31

- Шесть страниц услуг объединены с новой главной в одну editorial-систему без изменения URL, SEO-метаданных, Schema.org и WordPress-контента.
- Добавлены адаптивный service hero, договорный trust-блок и строка стандартов Nexor; существующие офферы, формы, телефоны и калькулятор сохранены.
- Унифицированы типографика, интервалы, карточки, изображения, FAQ и CTA для desktop, tablet и mobile.
- Добавлены отдельные baseline/local/production-аудиты шести страниц на 1440×900 и 390×844.

## 1.5.1 — 2026-07-31

- Опубликован редизайн главной и выполнена полная post-deploy проверка production.
- Уменьшены интервалы desktop-навигации на 1440 px: поиск больше не выходит из контейнера и не перекрывает телефон.
- Добавлены воспроизводимый production evidence-аудит и устойчивые HTTP/1.1-повторы для popup и слайдера ДО/ПОСЛЕ.

## 1.5.0 — 2026-07-30

- Главная собрана в единой editorial-системе на локальном Montserrat: новый hero, асимметричные услуги и проекты, premium-калькулятор и согласованный порядок секций.
- Блоки сметы, сроков, системы Nexor, пяти этапов, «до/после», шести дополнительных услуг и четырёх постоянных бонусов получили адаптивные интерактивные представления.
- Временный баннер дизайн-проекта показывает управляемый дедлайн без порога стоимости в публичном тексте; условия постоянных бонусов сохранены в модальных окнах.
- Добавлены keyboard/touch-управление, focus trap/restore, Escape, `prefers-reduced-motion`, lazy loading и IntersectionObserver-анимации.
- Добавлены production-like fixture и Chromium-аудиты для 1440/1024/768/390 px, popup desktop/mobile и новых интерактивов.

## 1.2.6 — 2026-07-16

- Исправлены четыре hero-фона, которые после React → WordPress переноса продолжали обращаться к несуществующему пути `/assets/`.
- Production crawler теперь проверяет также ресурсы из inline `background-image`.

## 1.2.5 — 2026-07-16

- Исправлены FAQ-сетки без якорного идентификатора на страницах услуг.
- Длинные CTA и непрерывные фрагменты юридического текста теперь корректно переносятся на мобильных экранах.

## 1.2.4 — 2026-07-16

- Аналогично исправлены двухколоночные FAQ-сетки на главной и страницах услуг, чтобы исключить горизонтальную прокрутку при 1024 px.

## 1.2.3 — 2026-07-16

- Скорректирован расчёт колонок блока «О компании»: межколоночный интервал теперь учитывается внутри доступной ширины контейнера.

## 1.2.2 — 2026-07-16

- Устранено горизонтальное переполнение блока «О компании» на планшетных и промежуточных разрешениях 1024–1280 px.

## 1.2.1 — 2026-07-16

- Исправлена невидимость содержимого раздела «О компании» при прямом переходе по якорю `#about-company-nexor`.

## 1.2.0 — 2026-07-16

- Исправлены дублирующиеся SEO title и description, появившиеся при первоначальном импорте.
- Технические архивы таксономий проектов удалены из sitemap и перенаправляются в каталог проектов.
- Для незаполненных menu locations созданы редактируемые главное и мобильное меню WordPress без изменения текущей навигации.
- Повторная активация плагина больше не перезаписывает SEO-данные существующих страниц.

## 1.1.3 — 2026-07-16

- Технические формулировки калькулятора заменены на понятные клиентские тексты.

## 1.1.2 — 2026-07-16

- Исправлены интервалы в шапке между логотипом, услугами, поиском и контактами.
- Полная desktop-навигация включается с 1440 px; на промежуточных ширинах используется компактное меню без горизонтального переполнения.
- Последний шаг калькулятора получил устойчивый выбор двух приоритетов, явное состояние готовности и корректную активацию кнопки результата.

## 1.1.1 — 2026-07-16

- Исправлены POSIX-права файлов и каталогов в релизном архиве, чтобы тема и плагин читались процессом веб-сервера.
- В релизный контроль добавлена проверка доступности файлов для непривилегированного пользователя.

## 1.1.0 — 2026-07-16

- Native WordPress navigation connected to migrated header with accessible desktop/mobile behavior.
- Added native safe search and noindex results template.
- Added managed service metadata, home services and curated service/project relations.
- Added disabled-by-default prices, video, additional services, promotions and exit-intent settings/sections.
- Added idempotent five-promotion seed and schema migration.
- Added stable-ID server validation, lead snapshots and Telegram context for offers.
- Preserved existing URLs, calculator formula, forms, project CPT and SEO generators.
